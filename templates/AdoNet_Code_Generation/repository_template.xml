<?xml version="1.0" encoding="utf-8"?>

<!--
	Copyright ©2002-2014 Daniel Bullington (dpbullington@gmail.com)
	Distributed under the MIT license: http://www.opensource.org/licenses/mit-license.php
-->
<Template xmlns="http://www.textmetal.com/api/v6.0.0">

	<OutputScope name="${ClrNamespace}\Repository.g.cs">
		<Include name="include_gen_cprt_message.cs.txt" />
<![CDATA[

using System;
using System.Collections.Generic;
using System.Data;
using System.IO;
using System.Reflection;
using System.Web;
using System.Xml;

using TextMetal.Common.Cerealization;
using TextMetal.Common.Core;
using TextMetal.Common.Data;
using TextMetal.Common.Data.Framework;
using TextMetal.Common.Syntax.Expressions;
using TextMetal.Common.Syntax.Operators;
using TextMetal.Common.Syntax.Statements;
]]>
		<If>
			<If.Condition>
				<UnaryExpression operator="IsDef">
					<UnaryExpression.TheExpression>
						<Aspect name="ClrUsingNamespaces" />
					</UnaryExpression.TheExpression>
				</UnaryExpression>
			</If.Condition>
			<If.True>
				<ForEach in="ClrUsingNamespaces" var-ct="_LoopCount" var-ix="_LoopIndex" var-item="_LoopItem">
					<ForEach.Body>
						<![CDATA[using ${_LoopItem};
]]>
					</ForEach.Body>
				</ForEach>
			</If.True>
		</If>

		<If>
			<If.Condition>
				<BinaryExpression operator="Eq">
					<BinaryExpression.LeftExpression>
						<Value type="System.Boolean" data="True" />
					</BinaryExpression.LeftExpression>
					<BinaryExpression.RightExpression>
						<Facet name="HasTables" />
					</BinaryExpression.RightExpression>
				</BinaryExpression>
			</If.Condition>
			<If.True>
				<![CDATA[using ${ClrNamespace}.Tables;
]]>
			</If.True>
		</If>
		<If>
			<If.Condition>
				<BinaryExpression operator="Eq">
					<BinaryExpression.LeftExpression>
						<Value type="System.Boolean" data="True" />
					</BinaryExpression.LeftExpression>
					<BinaryExpression.RightExpression>
						<Facet name="HasViews" />
					</BinaryExpression.RightExpression>
				</BinaryExpression>
			</If.Condition>
			<If.True>
				<![CDATA[using ${ClrNamespace}.Views;
]]>
			</If.True>
		</If>
		<If>
			<If.Condition>
				<BinaryExpression operator="Eq">
					<BinaryExpression.LeftExpression>
						<Value type="System.Boolean" data="True" />
					</BinaryExpression.LeftExpression>
					<BinaryExpression.RightExpression>
						<Facet name="HasProcedures" />
					</BinaryExpression.RightExpression>
				</BinaryExpression>
			</If.Condition>
			<If.True>
				<![CDATA[using ${ClrNamespace}.Procedures;
]]>
			</If.True>
		</If>
<![CDATA[
namespace ${ClrNamespace}
{
	public partial class Repository : ModelRepository, IRepository
	{
		#region Constructors/Destructors

		public Repository()
		{
		}

		#endregion

		#region Methods/Operators

		public override TModel CreateModel<TModel>()
		{
			Type modelType;

			modelType = typeof(TModel);

			if (false)
				return default(TModel);
]]>

	<ForEach in="Schemas" var-ct="_LoopCount" var-ix="_LoopIndex">
		<ForEach.Filter></ForEach.Filter>
		<ForEach.Body>

			<ForEach in="Tables" var-ct="_LoopCount" var-ix="_LoopIndex">
				<ForEach.Filter></ForEach.Filter>
				<ForEach.Body>
<![CDATA[
			else if (modelType == typeof(I${TableNameSingularPascalCase}))
				return (TModel)(object)new ${TableNameSingularPascalCase}();]]>
				</ForEach.Body>
			</ForEach>

		</ForEach.Body>
	</ForEach>

<![CDATA[
			else
				throw new InvalidOperationException(string.Format("Unknown model type '{0}'.", modelType.FullName));
		}

		public override TRequestModel CreateRequestModel<TRequestModel>()
		{
			Type requestModelType;

			requestModelType = typeof(TRequestModel);

			if (false)
				return default(TRequestModel);
]]>

	<ForEach in="Schemas" var-ct="_LoopCount" var-ix="_LoopIndex">
		<ForEach.Filter></ForEach.Filter>
		<ForEach.Body>

			<ForEach in="Procedures" var-ct="_LoopCount" var-ix="_LoopIndex">
				<ForEach.Filter></ForEach.Filter>
				<ForEach.Body>
<![CDATA[
			else if (requestModelType == typeof(I${ProcedureNameSingularPascalCase}_Request))
				return (TRequestModel)(object)new ${ProcedureNameSingularPascalCase}_Request();]]>
				</ForEach.Body>
			</ForEach>

		</ForEach.Body>
	</ForEach>

<![CDATA[
			else
				throw new InvalidOperationException(string.Format("Unknown request model type '{0}'.", requestModelType.FullName));
		}

		public override TResultModel CreateResultModel<TResultModel>()
		{
			Type resultModelType;

			resultModelType = typeof(TResultModel);

			if (false)
				return default(TResultModel);
]]>

	<ForEach in="Schemas" var-ct="_LoopCount" var-ix="_LoopIndex">
		<ForEach.Filter></ForEach.Filter>
		<ForEach.Body>

			<ForEach in="Procedures" var-ct="_LoopCount" var-ix="_LoopIndex">
				<ForEach.Filter></ForEach.Filter>
				<ForEach.Body>
<![CDATA[
			else if (resultModelType == typeof(I${ProcedureNameSingularPascalCase}_Result))
				return (TResultModel)(object)new ${ProcedureNameSingularPascalCase}_Result();]]>
				</ForEach.Body>
			</ForEach>

		</ForEach.Body>
	</ForEach>

<![CDATA[
			else
				throw new InvalidOperationException(string.Format("Unknown result model type '{0}'.", resultModelType.FullName));
		}

		public override TResponseModel CreateResponseModel<TResultModel, TResponseModel>()
		{
			Type responseModelType;

			responseModelType = typeof(TResponseModel);

			if (false)
				return default(TResponseModel);
]]>

	<ForEach in="Schemas" var-ct="_LoopCount" var-ix="_LoopIndex">
		<ForEach.Filter></ForEach.Filter>
		<ForEach.Body>

			<ForEach in="Procedures" var-ct="_LoopCount" var-ix="_LoopIndex">
				<ForEach.Filter></ForEach.Filter>
				<ForEach.Body>
<![CDATA[
			else if (responseModelType == typeof(I${ProcedureNameSingularPascalCase}_Response))
				return (TResponseModel)(object)new ${ProcedureNameSingularPascalCase}_Response();]]>
				</ForEach.Body>
			</ForEach>

		</ForEach.Body>
	</ForEach>

<![CDATA[
			else
				throw new InvalidOperationException(string.Format("Unknown response model type '{0}'.", responseModelType.FullName));
		}

		#endregion
	}
}
]]>
	</OutputScope>

</Template>
